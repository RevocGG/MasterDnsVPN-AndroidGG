package com.masterdnsvpn.ui.theme

import androidx.compose.ui.graphics.Color

// ── MasterDnsVPN Brand Palette ────────────────────────────────────────────────
// Extracted from app background (dark teal Islamic pattern) and logo (gold + teal griffin)

// Gold / Amber — from the border, badge, and logo accents
val GoldPrimary    = Color(0xFFC49A1A)   // deep gold border
val GoldLight      = Color(0xFFD4B84A)   // lighter gold highlight
val GoldDark       = Color(0xFF8B6914)   // shadowed gold
val GoldAccent     = Color(0xFFE8C84E)   // bright gold shine

// Teal — from background gradient and logo feathers
val TealPrimary    = Color(0xFF006070)   // mid teal
val TealLight      = Color(0xFF008B9A)   // lighter teal
val TealDark       = Color(0xFF003848)   // dark teal (background deep)
val TealDeep       = Color(0xFF002030)   // near-black teal
val CyanAccent     = Color(0xFF00C4CC)   // bright teal highlight

// Background shades — matches the background image dark teal
val DarkBg         = Color(0xFF002535)   // darkest background
val DarkSurface    = Color(0xFF003040)   // card surface
val DarkCard       = Color(0xFF003D50)   // elevated card

// Text on dark
val TextPrimary    = Color(0xFFE8D8A0)   // warm gold-white (matches border text)
val TextSecondary  = Color(0xFFB0C8C8)   // soft teal-white
val TextHint       = Color(0xFF6A9898)   // muted teal

// Status colors
val GreenOnline    = Color(0xFF2ECC71)
val RedError       = Color(0xFFE74C3C)
val AmberWarn      = Color(0xFFE8A020)
val PurpleDebug    = Color(0xFFAF7AC5)

// Glass / overlay effects
val GlassBg        = Color(0x22006070)   // teal tinted glass
val GlassBorder    = Color(0x55C49A1A)   // gold border overlay
val GlassHighlight = Color(0x18FFFFFF)   // subtle white shimmer